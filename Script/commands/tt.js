module.exports = {
  config: {
    name: "tt",
    version: "1.0",
    author: "MZ",
    countDown: 5,
    role: 0,
    shortDescription: "ইনবক্সে বটকে আনা",
    longDescription: "এই কমান্ড ব্যবহার করলে বট গ্রুপ থেকে সরাসরি ইনবক্সে চলে আসবে কোনো ঝামেলা ছাড়া",
    category: "utility",
    guide: {
      en: "Use {pn} to bring the bot to your inbox automatically."
    }
  },

  onStart: async function ({ api, event }) {
    const { senderID, threadID } = event;

    api.sendMessage("👋 তোমার ইনবক্সে বট আসছে, একটু অপেক্ষা করো...", senderID, (err) => {
      if (err) {
        // Fallback যদি কোনো কারণে ইনবক্সে মেসেজ না যায়
        api.sendMessage(
          "⚠️ ইনবক্সে আনার চেষ্টা সফল হয়নি। আগে বটকে Messenger-এ ‘Hi’ পাঠিয়ে রাখো, তারপর আবার চেষ্টা করো।",
          threadID
        );
      } else {
        // ইনবক্সে সফলভাবে আনতে পারা গেছে
        api.sendMessage(
          "🚀 বট এখন তোমার ইনবক্সে এসে গেছে, এখন তুমি সরাসরি ইনবক্সে সবকিছু করতে পারবে ✅",
          threadID
        );
      }
    });
  }
};
