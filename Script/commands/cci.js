module.exports = {
  config: {
    name: "cci",
    version: "1.0",
    author: "MZ",
    countDown: 5,
    role: 0,
    shortDescription: "বটকে ইনবক্সে নক করুন",
    longDescription: "এই কমান্ড ব্যবহার করে বটকে ইনবক্সে নক করানো যায়",
    category: "utility",
    guide: {
      en: "{pn}"
    }
  },

  onStart: async function ({ api, event }) {
    const { senderID, threadID } = event;

    api.sendMessage("👋 হ্যালো! আপনি গ্রুপ থেকে ইনবক্সে বটকে নক করেছেন ✅", senderID, (err) => {
      if (err) {
        api.sendMessage(
          "⚠️ ইনবক্সে নক পাঠানো যায়নি। Messenger-এ বটকে আগে 'Hi' বা '/' পাঠান, তারপর আবার চেষ্টা করুন।",
          threadID
        );
      } else {
        api.sendMessage(
          "📨 ইনবক্সে নক পাঠানো হয়েছে! এখন আপনি ইনবক্সে সব ফিচার ব্যবহার করতে পারবেন ✅",
          threadID
        );
      }
    });
  }
};
